{
  "OrgListType": {
    "description": "Represents a list of organizations.",
    "allOf": [
      { "$ref": "common.json#/ResourceType" },
      {
        "type": "object",
        "properties": {
          "org": {
            "type": "array",
            "description": "Reference to an organization.",
            "items": {
              "$ref": "common.json#/ReferenceType"
            }
          }
        },
        "required": ["org"]
      }
    ]
  }
}
